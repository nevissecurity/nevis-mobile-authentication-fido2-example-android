name: Get Version
description: Retrieves the version and build number of the app

outputs:
    version-number:
        description: "The current version"
        value: ${{ steps.get_version_number.outputs.versionName }}
    build-number:
        description: "The generated build number"
        value: ${{ steps.get_build_number.outputs.build_number }}

runs:
    using: composite

    steps:
        - name: Get Version Number
          id: get_version_number
          uses: ltDino/android-get-version-action@v1.0
          with:
              gradlePath: app/build.gradle.kts

        - name: Generate Build Number
          id: get_build_number
          uses: onyxmueller/build-tag-number@v1
          with:
              token: ${{ env.GITHUB_TOKEN }}
